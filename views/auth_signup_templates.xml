<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="signup_fields_extended" inherit_id="auth_signup.fields">
        <xpath expr="//div[hasclass('field-confirm_password')]" position="after">
            <!-- Captcha -->
            <div
                class="mb-3 field-captcha">
                <div class="g-recaptcha" t-att-data-sitekey="recaptcha_site_key" data-theme="light"></div>
                <div t-if="error and error.get('captcha')" class="alert alert-danger mt-2"
                    role="alert">
                    <t t-esc="error.get('captcha')" />
                </div>
            </div> Copy <!--
            Términos y condiciones -->
        <div
                class="mb-3 field-terms">
                <div class="form-check">
                    <input type="checkbox" name="accept_terms" id="accept_terms"
                        class="form-check-input" required="required" />
                    <label for="accept_terms" class="form-check-label"> Acepto los <a href="/terms"
                            target="_blank">términos y condiciones</a>
                    </label>
                </div>
                <div t-if="error and error.get('terms')" class="alert alert-danger mt-2"
                    role="alert">
                    <t t-esc="error.get('terms')" />
                </div>
            </div>

            <!-- Mensaje de verificación de correo -->
        <div
                class="alert alert-info mb-3">
                <i class="fa fa-info-circle me-2"></i> Se enviará un código de verificación a tu
            correo electrónico. </div>
        </xpath>
    </template>
</odoo>